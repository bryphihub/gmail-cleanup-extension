// QuickPresetsTab.jsx — no longer used.
//
// Quick Presets used to be its own tab, but we moved it to live inside the
// Search tab instead (as a row of buttons above the filters) since that's
// where the results/select/trash/archive flow already lives. This file is
// kept as a placeholder rather than deleted — the actual preset list now
// lives in presets.js, and the buttons are rendered in SearchTab.jsx.
